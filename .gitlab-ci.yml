image: node:11.7.0

cache:
  paths:
    - node_modules/

pages:
  script:
    - npm install
    - ./node_modules/.bin/gatsby build
    - cp ./keybase.txt public/keybase.txt
  artifacts:
    paths:
      - public
  only:
    - master
